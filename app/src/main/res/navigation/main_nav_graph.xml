<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph"
    app:startDestination="@id/mapFragment">

    <fragment
        android:id="@+id/mapFragment"
        android:name="com.elamparithi.parkypark.ui.map.MapFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_map" />

    <fragment
        android:id="@+id/booking_history"
        android:name="com.elamparithi.parkypark.ui.bookingHistory.BookingHistoryFragment"
        android:label="@string/booking_history"
        tools:layout="@layout/fragment_booking_history" />
</navigation>